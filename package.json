{
  "name": "no-mistakes-monitor",
  "version": "0.1.0",
  "description": "Monitor no-mistakes pipeline runs and Claude Code sessions across every repo on your machine, and jump to the window that wants you.",
  "type": "module",
  "bin": {
    "nmmon": "bin/nmmon.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "coverage": "node --test --experimental-test-coverage test/*.test.js"
  },
  "files": [
    "bin/",
    "src/",
    "public/",
    "hooks/",
    "README.md"
  ],
  "license": "UNLICENSED",
  "private": true
}
